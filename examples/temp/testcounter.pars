let counter = import(@./counter.pars)
log(counter.increment())  // 1
log(counter.increment())  // 2
log(counter.count)        // 0 (original value, not modified)
log(counter.getCount())   // 2 (updated value)