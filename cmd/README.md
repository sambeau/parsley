# cmd/

This directory contains the main applications for this project.

Example structure:
```
cmd/
├── pars/
│   └── main.go
└── parser-cli/
    └── main.go
```

Each subdirectory should contain a main.go file that serves as the entry point for that specific application.
