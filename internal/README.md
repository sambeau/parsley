# internal/

This directory contains private packages that are specific to this application and should not be imported by external applications.

Example structure:
```
internal/
├── config/
│   └── config.go
├── handlers/
│   └── parser_handler.go
└── models/
    └── ast_models.go
```
